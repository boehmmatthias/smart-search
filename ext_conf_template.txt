# cat=LLM/basic; type=string; label=Embedding server URL (llama-server --embedding, default port 8080)
embeddingServerUrl = http://localhost:8080

# cat=LLM/basic; type=string; label=Generation server URL (llama-server for chat, default port 8081)
generationServerUrl = http://localhost:8081

# cat=LLM/basic; type=integer; label=Max tokens for generated answer
generationMaxTokens = 512

# cat=LLM/basic; type=integer; label=HTTP timeout in seconds for generation requests (CPU inference is slow; increase if answers get cut off)
generationTimeout = 300

# cat=LLM/basic; type=integer; label=Maximum characters of text sent to the embedding server (keep in sync with --ctx-size; ~4 chars per token for typical prose)
embeddingContextLength = 6000

# cat=LLM/RAG; type=integer; label=Number of top documents to retrieve for RAG context (advisory: read and applied by consuming extensions, not by smart_search)
ragTopK = 5

# cat=LLM/RAG; type=integer; label=Maximum characters of document content included per RAG context block (advisory: read and applied by consuming extensions, not by smart_search)
documentContextLength = 800

# cat=LLM/RAG; type=string; label=Minimum cosine similarity score (0.0-1.0) for semantic results (advisory: read and applied by consuming extensions, not by smart_search)
semanticThreshold = 0.30

# cat=LLM/RAG; type=text; label=Custom system prompt for the generation service (leave empty to use the built-in default)
systemPrompt =

# cat=LLM/Performance; type=integer; label=TTL in seconds for cached search results (0 disables query caching)
queryCacheTtl = 3600

# cat=Providers/basic; type=options[llama.cpp=llamacpp,Ollama=ollama,OpenAI=openai]; label=Embedding backend
embeddingProvider = llamacpp

# cat=Providers/basic; type=options[llama.cpp=llamacpp,Ollama=ollama,OpenAI=openai]; label=Generation backend
generationProvider = llamacpp

# cat=Providers/openai; type=string; label=OpenAI API key (stored in plain text in the site configuration)
openAiApiKey =

# cat=Providers/openai; type=string; label=OpenAI embedding model
openAiEmbeddingModel = text-embedding-3-small

# cat=Providers/openai; type=string; label=OpenAI generation model
openAiGenerationModel = gpt-4o-mini

# cat=Providers/ollama; type=string; label=Ollama server URL
ollamaServerUrl = http://localhost:11434

# cat=Providers/ollama; type=string; label=Ollama embedding model
ollamaEmbeddingModel = nomic-embed-text

# cat=Providers/ollama; type=string; label=Ollama generation model
ollamaGenerationModel = llama3.2
